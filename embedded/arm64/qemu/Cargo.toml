[package]
name = "wra-arm64-qemu-demo"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[workspace]

[lib]
path = "src/lib.rs"

[dependencies]
wra-embedded = { path = "../../../crates/wra-embedded" }
wra-signal = { path = "../../../crates/wra-signal" }
